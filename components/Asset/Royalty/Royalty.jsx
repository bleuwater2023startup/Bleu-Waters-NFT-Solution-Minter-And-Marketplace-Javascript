import classes from "./Royalty.module.css";

const Royalty = () => {
  return (
    <div className={classes.container}>
      Royalty splits is automatically deposited into each recipient’s wallet
      address.
    </div>
  );
};

export default Royalty;
